// Base
export * from './base/base.entity';

// Master Data
export * from './master-data';

// User Access
// (will be added as entities are created)

// HR
// (will be added as entities are created)

// Inventory
// (will be added as entities are created)

// Building
// (will be added as entities are created)

// Mess
// (will be added as entities are created)

// Audit
// (will be added as entities are created)